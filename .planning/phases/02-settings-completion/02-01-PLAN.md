---
phase: 02-settings-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sinaloka-platform/src/pages/Settings/index.tsx
  - sinaloka-platform/src/pages/Settings/useSettingsPage.ts
  - sinaloka-platform/src/pages/Settings/tabs/GeneralTab.tsx
  - sinaloka-platform/src/pages/Settings/tabs/BrandingTab.tsx
  - sinaloka-platform/src/pages/Settings/tabs/SecurityTab.tsx
  - sinaloka-platform/src/pages/Settings/tabs/IntegrationsTab.tsx
autonomous: false
requirements:
  - BRND-01
  - SETT-01
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "Settings page shows exactly 3 tabs: General, Billing, Academic"
    - "No Branding, Security, or Integrations tabs are visible"
    - "General tab has no Delete Institution button or Danger Zone section"
    - "Settings page loads and all 3 remaining tabs function correctly"
  artifacts:
    - path: "sinaloka-platform/src/pages/Settings/index.tsx"
      provides: "Settings page with only 3 tabs"
      contains: "general.*billing.*academic"
    - path: "sinaloka-platform/src/pages/Settings/tabs/GeneralTab.tsx"
      provides: "General tab without danger zone"
  key_links:
    - from: "sinaloka-platform/src/pages/Settings/index.tsx"
      to: "tabs array"
      via: "tab definitions"
      pattern: "general.*billing.*academic"
---

<objective>
Remove all non-functional Settings tabs (Branding, Security, Integrations) and the Delete Institution danger zone from the General tab. After this plan, Settings has exactly 3 working tabs: General, Billing, Academic.

Purpose: Eliminate misleading UI — every visible control must be functional.
Output: Clean Settings page with only functional tabs.
</objective>

<execution_context>
@/home/zet/.claude/get-shit-done/workflows/execute-plan.md
@/home/zet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-completion/02-CONTEXT.md

<interfaces>
<!-- Current Settings/index.tsx tab structure (to be modified) -->

From sinaloka-platform/src/pages/Settings/index.tsx:
```typescript
// Current imports to REMOVE:
import { BrandingTab } from './tabs/BrandingTab';
import { SecurityTab } from './tabs/SecurityTab';
import { IntegrationsTab } from './tabs/IntegrationsTab';

// Current icon imports — remove Palette, ShieldCheck, Puzzle:
import { Building2, CreditCard, Palette, GraduationCap, ShieldCheck, Puzzle } from 'lucide-react';

// Current tabs array — remove branding, security, integrations entries:
const tabs = [
  { id: 'general', label: t('settings.tabs.general'), icon: Building2 },
  { id: 'billing', label: t('settings.tabs.billing'), icon: CreditCard },
  { id: 'branding', label: t('settings.tabs.branding'), icon: Palette },        // REMOVE
  { id: 'academic', label: t('settings.tabs.academic'), icon: GraduationCap },
  { id: 'security', label: t('settings.tabs.security'), icon: ShieldCheck },     // REMOVE
  { id: 'integrations', label: t('settings.tabs.integrations'), icon: Puzzle },  // REMOVE
];
```

From sinaloka-platform/src/pages/Settings/useSettingsPage.ts:
```typescript
// Line 12: remove primaryColor state
const [primaryColor, setPrimaryColor] = useState('#0f172a');

// Return object: remove primaryColor, setPrimaryColor
```

From sinaloka-platform/src/pages/Settings/tabs/GeneralTab.tsx:
```typescript
// Lines 91-103: Danger Zone card to REMOVE (entire second <Card> block)
// Also remove: Trash2, AlertCircle from lucide imports
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove non-functional tabs and danger zone</name>
  <files>
    sinaloka-platform/src/pages/Settings/index.tsx,
    sinaloka-platform/src/pages/Settings/useSettingsPage.ts,
    sinaloka-platform/src/pages/Settings/tabs/GeneralTab.tsx,
    sinaloka-platform/src/pages/Settings/tabs/BrandingTab.tsx,
    sinaloka-platform/src/pages/Settings/tabs/SecurityTab.tsx,
    sinaloka-platform/src/pages/Settings/tabs/IntegrationsTab.tsx
  </files>
  <read_first>
    sinaloka-platform/src/pages/Settings/index.tsx,
    sinaloka-platform/src/pages/Settings/useSettingsPage.ts,
    sinaloka-platform/src/pages/Settings/tabs/GeneralTab.tsx,
    sinaloka-platform/src/pages/Settings/tabs/BrandingTab.tsx,
    sinaloka-platform/src/pages/Settings/tabs/SecurityTab.tsx,
    sinaloka-platform/src/pages/Settings/tabs/IntegrationsTab.tsx
  </read_first>
  <action>
    **Delete 3 component files entirely:**
    - Delete `sinaloka-platform/src/pages/Settings/tabs/BrandingTab.tsx`
    - Delete `sinaloka-platform/src/pages/Settings/tabs/SecurityTab.tsx`
    - Delete `sinaloka-platform/src/pages/Settings/tabs/IntegrationsTab.tsx`

    **Edit Settings/index.tsx:**
    1. Remove imports for BrandingTab, SecurityTab, IntegrationsTab
    2. Remove icon imports: Palette, ShieldCheck, Puzzle (keep Building2, CreditCard, GraduationCap)
    3. Remove 3 entries from `tabs` array — keep only general, billing, academic (in that order)
    4. Remove 3 case blocks from `renderTabContent()` switch — remove `case 'branding'`, `case 'security'`, `case 'integrations'`

    **Edit useSettingsPage.ts:**
    1. Remove `primaryColor` state declaration (line 12: `const [primaryColor, setPrimaryColor] = useState('#0f172a');`)
    2. Remove `primaryColor` and `setPrimaryColor` from the return object (around lines 297-298)

    **Edit GeneralTab.tsx:**
    1. Remove the entire Danger Zone `<Card>` block (lines 91-103 — the second Card with `border-rose-100` class)
    2. Remove unused imports: `Trash2` and `AlertCircle` from lucide-react imports (line 3)
    3. Keep the `Save` icon import (still used by the save button)
  </action>
  <verify>
    <automated>cd /home/zet/Project/sinaloka/sinaloka-platform && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BrandingTab\|SecurityTab\|IntegrationsTab" sinaloka-platform/src/pages/Settings/index.tsx` returns 0
    - `grep -c "Palette\|ShieldCheck\|Puzzle" sinaloka-platform/src/pages/Settings/index.tsx` returns 0
    - `grep -c "primaryColor\|setPrimaryColor" sinaloka-platform/src/pages/Settings/useSettingsPage.ts` returns 0
    - `grep -c "dangerZone\|deleteInstitution\|Trash2\|AlertCircle" sinaloka-platform/src/pages/Settings/tabs/GeneralTab.tsx` returns 0
    - `test ! -f sinaloka-platform/src/pages/Settings/tabs/BrandingTab.tsx` passes
    - `test ! -f sinaloka-platform/src/pages/Settings/tabs/SecurityTab.tsx` passes
    - `test ! -f sinaloka-platform/src/pages/Settings/tabs/IntegrationsTab.tsx` passes
    - `grep -c "{ id:" sinaloka-platform/src/pages/Settings/index.tsx` returns exactly 3
  </acceptance_criteria>
  <done>Settings/index.tsx has exactly 3 tabs (general, billing, academic), 3 tab component files deleted, no primaryColor state, no danger zone in GeneralTab, TypeScript compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Settings page works with 3 tabs</name>
  <files>sinaloka-platform/src/pages/Settings/index.tsx</files>
  <action>
    Human verifies the Settings page visually. No automated action — this is a visual/functional checkpoint after Task 1 removals.
  </action>
  <read_first>sinaloka-platform/src/pages/Settings/index.tsx</read_first>
  <what-built>Removed Branding, Security, and Integrations tabs from Settings. Removed Delete Institution button from General tab. Settings now shows exactly 3 functional tabs: General, Billing, Academic.</what-built>
  <how-to-verify>
    1. Start the platform dev server: `cd sinaloka-platform && npm run dev`
    2. Navigate to Settings page (http://localhost:3000 -> Settings in sidebar)
    3. Verify exactly 3 tabs in the sidebar: General, Billing, Academic
    4. Confirm NO Branding, Security, or Integrations tabs are visible
    5. Click General tab — verify it loads with institution info form but NO "Danger Zone" / "Delete Institution" section at the bottom
    6. Click Billing tab — verify it loads normally
    7. Click Academic tab — verify it loads with rooms, subjects, grades, working days
    8. Verify no console errors in browser DevTools
  </how-to-verify>
  <verify>
    <automated>cd /home/zet/Project/sinaloka/sinaloka-platform && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Production build succeeds with no errors
    - Human confirms 3 tabs visible, no removed tabs present, no danger zone
  </acceptance_criteria>
  <done>Human approves that Settings page shows exactly 3 working tabs with no non-functional UI</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation: `cd sinaloka-platform && npx tsc --noEmit` passes with no errors
- Only 3 tab files remain in `sinaloka-platform/src/pages/Settings/tabs/`: GeneralTab.tsx, BillingTab.tsx, AcademicTab.tsx
- No references to removed components anywhere in Settings directory
- Build succeeds: `cd sinaloka-platform && npm run build`
</verification>

<success_criteria>
- Settings page shows exactly 3 tabs: General, Billing, Academic
- No Branding, Security, or Integrations tabs exist in code or UI
- General tab has no Delete Institution button
- All 3 remaining tabs function correctly
- TypeScript compiles without errors
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-completion/02-01-SUMMARY.md`
</output>
