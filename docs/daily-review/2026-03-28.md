# Review PR Harian — 2026-03-28

> Periode review: PR yang di-merge pada **2026-03-27**
> Reviewer: Claude Code (automated senior engineer review)

---

## Ringkasan

| Metrik | Nilai |
|--------|-------|
| Total PR direview | 5 |
| Total file berubah | ~50 (23 + 5 + 1 + 8 + 9) |
| Total penambahan baris | ~2.679 |
| Total penghapusan baris | ~210 |
| Estimasi celah test | **14** fungsi/endpoint |
| PR berisiko tinggi | 2 (PR #115 keamanan, PR #113 migrasi DB) |

---

## Daftar PR yang Direview

---

### PR #113: feat(platform): add landing page template selection

- **Author**: ghifarizuhir
- **Perubahan**: +2.379 / -72 baris
- **File terdampak**:
  - `sinaloka-backend/prisma/schema.prisma`
  - `sinaloka-backend/prisma/migrations/20260327100000_add_landing_template/migration.sql`
  - `sinaloka-backend/src/modules/settings/settings.dto.ts`
  - `sinaloka-backend/src/modules/settings/settings.service.ts`
  - `sinaloka-backend/src/modules/institution/institution.service.ts`
  - `sinaloka-platform/src/pages/public/templates/template-config.ts` *(baru)*
  - `sinaloka-platform/src/pages/Settings/tabs/components/TemplateSelector.tsx` *(baru)*
  - 8 komponen landing page yang direfactor
  - `sinaloka-platform/src/types/landing.ts`
- **Tingkat risiko**: **Tinggi** — migrasi DB + refactor 8 komponen frontend
- **Isu ditemukan**:
  1. **[Medium] `landing_template` disimpan sebagai `String?` di Prisma, bukan Enum.** Validasi hanya ada di layer Zod DTO. Jika nilai invalid masuk langsung ke DB (misalnya via script manual), `getTemplateConfig()` akan menerima ID yang tidak dikenal dan berpotensi crash atau fallback silent. Sebaiknya tambahkan fallback eksplisit di `getTemplateConfig()` jika template ID tidak ditemukan.
  2. **[Medium] Tidak ada unit test untuk perubahan `settings.service.ts` dan `institution.service.ts`.** `getLandingSettings`, `updateLandingSettings`, dan `findLandingBySlug` kini menyertakan `landing_template` dalam select/response, namun tidak ada test case yang memverifikasi field ini.
  3. **[Rendah] Duplikasi `collectChanges()` di `LandingTab.tsx`.** Dipanggil dua kali — sekali di `handleSave` untuk deteksi perubahan, sekali di props `changes` pada modal konfirmasi. Logika ini rentan terhadap ketidakkonsistenan jika salah satu berubah tanpa update yang lain.
  4. **[Info] Migration SQL backward compatible** — menambahkan `VARCHAR(30) DEFAULT 'bold-geometric'`, sehingga institusi yang sudah ada tidak terdampak secara visual.

---

### PR #114: fix(platform): show template change in confirmation modal

- **Author**: ghifarizuhir
- **Perubahan**: +10 / -7 baris
- **File terdampak**:
  - `sinaloka-platform/src/pages/Settings/tabs/LandingTab.tsx`
- **Tingkat risiko**: **Rendah** — perbaikan UI logic satu file
- **Isu ditemukan**:
  - **[Rendah] Pengecekan template redundan dihapus dari `handleSave` early return.** Sekarang semua deteksi perubahan (termasuk template) diproses melalui `detectScalarChange`. Ini sudah benar, namun jika `getTemplateConfig()` melempar exception untuk ID unknown (lihat isu PR #113 poin 1), `handleSave` akan crash tanpa error handling.
  - Tidak ditemukan isu kritis lainnya.

---

### PR #115: fix(auto-security): add tenant isolation to tutor endpoints and fix XSS

- **Author**: ghifarizuhir
- **Perubahan**: +56 / -56 baris (net: 0 — murni penambahan `institutionId` parameter)
- **File terdampak**:
  - `sinaloka-backend/src/modules/session/session.service.ts`
  - `sinaloka-backend/src/modules/session/tutor-session.controller.ts`
  - `sinaloka-backend/src/modules/attendance/attendance.service.ts`
  - `sinaloka-backend/src/modules/attendance/tutor-attendance.controller.ts`
  - `sinaloka-platform/src/pages/Classes/ClassDeleteModal.tsx`
- **Tingkat risiko**: **Tinggi** — perbaikan keamanan kritis pada tenant isolation dan XSS
- **Isu ditemukan**:
  1. **[High] Penggunaan `user.institutionId!` (non-null assertion) di controller.** Di `tutor-session.controller.ts` dan `tutor-attendance.controller.ts`, semua pemanggilan service kini menggunakan `user.institutionId!`. Jika JWT tutor malformed atau `TenantInterceptor` gagal mengisi `institutionId`, operator `!` akan meneruskan `undefined` ke service tanpa error awal yang jelas. Service akan mengirim `institution_id: undefined` ke Prisma, yang mungkin tidak akan menyaring data dengan benar. Sebaiknya tambahkan guard eksplisit atau gunakan `@InstitutionId()` decorator yang sudah ada di admin controller.
  2. **[High] Tidak ada unit test baru untuk 4 method service yang kritis.** `requestReschedule`, `cancelSession`, `getSessionStudents`, `completeSession` di `SessionService`, serta `batchCreate` dan `updateByTutor` di `AttendanceService` — semuanya kini memiliki logika tenant isolation tetapi tanpa test case yang memverifikasi isolasi tersebut benar-benar bekerja (misalnya tutor dari institusi A tidak bisa mengakses sesi institusi B).
  3. **[Medium] Temuan yang belum diperbaiki (dari PR description):** TOCTOU pada 11 instance di `StudentService`, `EnrollmentService`, dll. File `.env.test` berisi JWT secret test yang ter-commit ke repo (`e2e-integration/.env.test`). Secret test harus di-rotate atau file tersebut di-gitignore dan dihapus dari history.
  4. **[Positif] XSS fix sudah benar** — `dangerouslySetInnerHTML` diganti dengan `<Trans>` component yang aman, mengikuti pola yang sudah dipakai di `StudentsDeleteModal`.

---

### PR #116: fix(enrollment): sprint 1 quick wins from audit ENR-01 to ENR-15

- **Author**: ghifarizuhir
- **Perubahan**: +68 / -20 baris
- **File terdampak**:
  - `sinaloka-backend/src/modules/enrollment/enrollment.service.ts`
  - `sinaloka-backend/src/modules/enrollment/enrollment.controller.ts`
  - `sinaloka-backend/src/modules/enrollment/enrollment.dto.ts`
  - `sinaloka-backend/src/modules/enrollment/enrollment.service.spec.ts`
  - `sinaloka-platform/src/pages/Enrollments/EnrollmentTable.tsx`
  - `sinaloka-platform/src/pages/Enrollments/useEnrollmentsPage.ts`
  - `sinaloka-platform/src/locales/en.json`
  - `sinaloka-platform/src/locales/id.json`
- **Tingkat risiko**: **Sedang** — perbaikan bug kritis (FK violation) dan keamanan (validasi input)
- **Isu ditemukan**:
  1. **[Medium] CSV import: tidak ada validasi MIME type file.** File size limit (1MB) sudah ditambahkan, tetapi `FileInterceptor` tidak memvalidasi bahwa file yang diupload benar-benar bertipe CSV. Penyerang bisa mengupload file berbahaya (misal executable) dengan ekstensi `.csv`. Sebaiknya tambahkan `fileFilter` di konfigurasi `FileInterceptor`.
  2. **[Positif] Fix FK violation dengan `$transaction`** sudah benar — payment dihapus dulu, baru enrollment dihapus. Unit test juga sudah diperbarui untuk memverifikasi `$transaction` dipanggil.
  3. **[Positif] `enrolled_at` timestamp fix** sudah benar — sebelumnya invoice generator menggunakan `new Date()` bukan `dto.enrolled_at`, sekarang konsisten menggunakan variabel `enrolledAt`.
  4. **[Positif] `EnrollmentExportQuerySchema`** menggantikan `any` — fix keamanan yang bagus untuk mencegah parameter injeksi di export endpoint.

---

### PR #117: fix(enrollment): sprint 2 — search, stats endpoint, autoInvoice toggle

- **Author**: ghifarizuhir
- **Perubahan**: +157 / -23 baris
- **File terdampak**:
  - `sinaloka-backend/src/modules/enrollment/enrollment.service.ts`
  - `sinaloka-backend/src/modules/enrollment/enrollment.controller.ts`
  - `sinaloka-backend/src/modules/enrollment/enrollment.dto.ts`
  - `sinaloka-backend/src/modules/enrollment/enrollment.service.spec.ts`
  - `sinaloka-platform/src/hooks/useEnrollments.ts`
  - `sinaloka-platform/src/pages/Enrollments/EnrollmentStats.tsx`
  - `sinaloka-platform/src/pages/Enrollments/index.tsx`
  - `sinaloka-platform/src/pages/Enrollments/useEnrollmentsPage.ts`
  - `sinaloka-platform/src/services/enrollments.service.ts`
- **Tingkat risiko**: **Sedang** — endpoint baru + perubahan logika financial (autoInvoice)
- **Isu ditemukan**:
  1. **[Medium] `getStats()` endpoint tidak terlihat memiliki role guard eksplisit di diff.** Perlu dipastikan endpoint `GET /admin/enrollments/stats` hanya bisa diakses ADMIN/SUPER_ADMIN (bukan TUTOR/PARENT). Jika controller sudah menggunakan `@Roles()` di class level, ini aman. Perlu verifikasi.
  2. **[Medium] Kombinasi filter `search` + `student_id`/`class_id` berpotensi confusing.** Saat `search` aktif bersamaan dengan `student_id`, query menghasilkan: `student_id = X AND (student.name CONTAINS ... OR class.name CONTAINS ...)`. Meskipun secara logika Prisma ini benar, behavior ini tidak didokumentasikan dan bisa membingungkan pengguna jika mereka mengisi search tapi hasil tetap kosong karena filter student_id.
  3. **[Positif] Urutan route sudah benar**: `@Get('stats')` ditempatkan sebelum `@Get(':id')` di controller, sehingga NestJS tidak salah memparsing `stats` sebagai UUID route param.
  4. **[Positif] Cache invalidation lengkap** — semua mutation hooks (create, update, delete, bulkUpdate, bulkDelete, import) kini juga invalidate `enrollment-stats`.
  5. **[Positif] 4 unit test baru** ditambahkan untuk autoInvoice (NEW vs PENDING) dan search (filter aktif + hasil kosong).

---

## Analisis Celah Test

### Prioritas Tinggi — Test hilang pada jalur kritis

| File | Fungsi / Endpoint | Saran Test |
|------|------------------|------------|
| `session.service.ts` | `requestReschedule()` | Test: tutor dari institusi A mencoba reschedule sesi institusi B → harus 404/403 |
| `session.service.ts` | `cancelSession()` | Test: verifikasi session lookup dengan `institution_id` disertakan di where clause |
| `session.service.ts` | `getSessionStudents()` | Test: enrollment & attendance query disaring oleh `institution_id` |
| `session.service.ts` | `completeSession()` | Test: class lookup untuk fee snapshot menggunakan `institution_id` |
| `attendance.service.ts` | `batchCreate()` | Test: session dari institusi lain tidak bisa diakses tutor cross-tenant |
| `attendance.service.ts` | `updateByTutor()` | Test: attendance dari institusi lain tidak bisa diupdate |
| `enrollment.controller.ts` | `GET /admin/enrollments/stats` | Test: endpoint hanya bisa diakses ADMIN role (unauthorized tutor → 403) |

### Prioritas Sedang — Coverage rendah pada business logic

| File | Fungsi / Endpoint | Saran Test |
|------|------------------|------------|
| `settings.service.ts` | `getLandingSettings()` | Test: field `landing_template` dikembalikan dalam response |
| `settings.service.ts` | `updateLandingSettings()` | Test: update `landing_template` ke nilai valid berhasil disimpan |
| `institution.service.ts` | `findLandingBySlug()` | Test: `landing_template` disertakan dalam response public API |
| `enrollment.service.ts` | `findAll()` dengan search | Test: kombinasi `search` + `student_id` filter — verifikasi perilaku AND logic |
| `enrollment.controller.ts` | `POST /import` | Test: upload file non-CSV (misal JPEG) → harus ditolak |
| `enrollment.service.ts` | `getStats()` | Test: stats mengembalikan count akurat per status; test dengan data kosong (0 semua) |

### Prioritas Rendah — Perubahan konfigurasi atau tipe

| File | Perubahan | Catatan |
|------|-----------|---------|
| `sinaloka-platform/src/types/landing.ts` | Tambah `landing_template` ke 3 interface | Type-only, tidak perlu test runtime |
| `sinaloka-platform/src/locales/en.json` + `id.json` | Tambah key `deleteTitle` | Verifikasi i18n key lewat existing E2E atau build check |
| `ClassDeleteModal.tsx` | Ganti `dangerouslySetInnerHTML` dengan `<Trans>` | Pertimbangkan E2E snapshot test untuk verifikasi rendering |

---

## Tindak Lanjut

### Keamanan (segera)

- [ ] **Investigasi `e2e-integration/.env.test`** — file ini ter-commit dengan JWT secret test. Evaluasi apakah perlu di-rotate dan di-gitignore.
- [ ] **Ganti `user.institutionId!` dengan guard eksplisit** di `tutor-session.controller.ts` dan `tutor-attendance.controller.ts`. Pertimbangkan menggunakan `@InstitutionId()` decorator yang sudah ada, atau lempar `ForbiddenException` jika null.
- [ ] **Tambahkan `fileFilter` ke CSV import** untuk memvalidasi MIME type (`text/csv`, `application/csv`) agar file non-CSV ditolak.

### Test yang perlu ditulis (backend)

- [ ] `session.service.spec.ts` → test cross-tenant isolation untuk 4 method tutor: `requestReschedule`, `cancelSession`, `getSessionStudents`, `completeSession`
- [ ] `attendance.service.spec.ts` → test cross-tenant isolation untuk `batchCreate` dan `updateByTutor`
- [ ] `enrollment.service.spec.ts` → test `getStats()` dengan berbagai skenario data
- [ ] `enrollment.service.spec.ts` → test `findAll()` dengan kombinasi `search` + filter lain
- [ ] `enrollment.controller.spec.ts` → test role guard pada endpoint `GET /admin/enrollments/stats`

### Test yang perlu ditulis (frontend / E2E)

- [ ] `e2e/` → Test Playwright: ganti template landing → simpan → verifikasi landing page publik berubah visual
- [ ] `e2e/` → Test Playwright: modal delete class menampilkan nama class dengan benar (tanpa HTML injection)
- [ ] Tambah fallback handling di `getTemplateConfig()` untuk template ID yang tidak dikenal (throw atau return default)

### Teknis lain

- [ ] **PR #115 — 11 TOCTOU vulnerabilities** yang dilaporkan tapi belum diperbaiki. Buat GitHub issue terpisah untuk tracking: `StudentService`, `EnrollmentService`, `ExpenseService`, `InvoiceService`, `PayoutSlipService`.
- [ ] **`SubjectService.delete` dan `AcademicYearService.deleteSemester`** — count query tanpa tenant scope (dilaporkan PR #115). Buat issue untuk perbaikan.

---

*Dokumen ini di-generate secara otomatis oleh Claude Code pada 2026-03-28. Semua temuan perlu divalidasi manual sebelum dibuat PR perbaikan.*
